import { createSlice } from '@reduxjs/toolkit';

export const sessionUserSlice = createSlice({
    name: 'sessionUser',
    initialState: '',
    reducers: {
        
    }
})

export const {  } = sessionUserSlice.actions;

export default sessionUserSlice.reducer;
